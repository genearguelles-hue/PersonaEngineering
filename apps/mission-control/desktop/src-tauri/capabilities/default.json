{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Mission Control window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
